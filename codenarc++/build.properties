# The 3 properties prefixed by "gw" should not be changed by customers
gw.branch.req.ant.version=1.9.3
gw.branch.req.java.version=1.8
gw.app=cc
gw.gosu-codenarc = C:/workspace/ClaimCenter904/modules/codenarc++/tools/gosu-codenarc
gw.gosu-codenarc.date = 2017.07.01 23:59
# If you check base files into source control, define a regular expression
# pattern for paths of source control metadata files (using forward slashes)
# that the checksum ought to skip
# For example, for CVS, use the following:
# scm.regex = (^|.*/)CVS(/.*|$)
# Or, if you use SVN, use the following:
# scm.regex = (^|.*/)\.svn(/.*|$)
